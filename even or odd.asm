test ax, 1           ; Test if the number is odd (AX & 1)
    jz skip              ; If even, skip it

test ax, 1           ; Test if the number is odd (AX & 1)
    jnz skip             ; If odd, skip it
